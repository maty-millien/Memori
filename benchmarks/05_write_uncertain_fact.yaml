id: 05_write_uncertain_fact
type: memory_tool_call
initial_memories: []
turns:
  - role: user
    content: I think the final presentation is on Friday, but I am not sure yet.
expected:
  tool_calls:
    - name: memory.write
      arguments:
        content_regex: (?i)(?=.*\bpresentation\b)(?=.*\bFriday\b)(?=.*\b(uncertain|unsure|not sure|might|maybe|possibly|tentative|not confirmed)\b)
  forbidden_tool_calls:
    - name: memory.update
    - name: memory.delete
  final_memory_count:
    min: 1
    max: 1
